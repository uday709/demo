import 'package:flutter/material.dart';

class Demo_p extends StatelessWidget {
  const Demo_p({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: App,
    );
  }
}
